// Illustrate ways how named function expressions
// can be created in JS 

var clickHandler = function() {
    // anonymous function (without identifier)
};

var keyHandler = function keyHandler() {
    // named func expression
};